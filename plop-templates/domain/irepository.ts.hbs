import { {{pascalCase name}} } from '../entities/{{kebabCase name}}.entity';

export interface I{{pascalCase name}}Repository {
  // Déclarez ici les méthodes du repository
  // Exemple:
  // create(data: Create{{pascalCase name}}Dto): Promise<{{pascalCase name}}>;
  // findById(id: string): Promise<{{pascalCase name}} | null>;
  // update(id: string, data: Update{{pascalCase name}}Dto): Promise<{{pascalCase name}} | null>;
  // delete(id: string): Promise<boolean>;
  // findAll(): Promise<{{pascalCase name}}[]>;
}
